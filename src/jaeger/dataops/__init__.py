"""Data operations package for Jaeger.

Handles dataset loading, conversion, splitting, and batching pipelines.
This package is the single entry point for all data I/O operations that
are not sequence-specific (which live in ``jaeger.seqops``).
"""
